// Re-exports the shared AIChatPanel for backwards compatibility within this folder.
// New code should import directly from @/components/shared/AIChatPanel.
export { default, type ChatMessage, type FocusItem } from "@/components/shared/AIChatPanel";
