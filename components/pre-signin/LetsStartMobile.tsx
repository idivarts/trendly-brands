import LetsStartAI from "@/components/pre-signin/LetsStartAI";
import React from "react";

// The lets-start experience is now a single responsive AI-input page.
// Kept as a named entry so the existing route wrapper imports stay valid.
const LetsStartMobile = () => <LetsStartAI />;

export default LetsStartMobile;
