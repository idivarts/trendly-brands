// Web stub — OTA updates are native-only, and this file keeps `expo-updates`
// out of the web bundle entirely (Metro resolves the .native variant on device).
export const OTAUpdateGate = () => null;

export default OTAUpdateGate;
